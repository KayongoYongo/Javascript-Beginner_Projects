let myString = "I am a Javascript developer";
/* Javascript has a string slicing method called slice()
   The slice() method takes two parameters: the starting index (inclusive) and the ending index (exclusive) of the slice. 
   If the ending index is not provided, the slice extends to the end of the string.
*/
console.log(myString.slice(2, 17));